const userinfo = {

  name : 'Sakib',
  ID : 25334,
  Area : 'Daka'


}
userinfo.Area = "Banani"

console.log(userinfo);

// When we use const with an object, 
// the reference to the object is fixed in memory,
//  but the properties inside the object remain mutable, 
// so they can still be changed. 